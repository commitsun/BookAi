version: "3.8"
services:
  hotelai:
    build: .
    container_name: hotelai
    tty: true
    stdin_open: true   # 🔹 permite input interactivo
    env_file:
      - .env           # 🔹 carga tu API key
    volumes:
      - .:/app
