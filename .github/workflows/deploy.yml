name: 🚀 Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🧰 Checkout repository
        uses: actions/checkout@v4

      - name: 🔐 Configure SSH access
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2_key
          chmod 600 ~/.ssh/ec2_key
          cat > ~/.ssh/config <<EOF
          Host ec2
            HostName ${{ secrets.EC2_HOST }}
            User ${{ secrets.EC2_USER }}
            IdentityFile ~/.ssh/ec2_key
            StrictHostKeyChecking no
          EOF

      - name: 📦 Sync repository to EC2
        run: |
          echo "🔄 Syncing project files to EC2..."
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/ec2_key -o StrictHostKeyChecking=no" \
            ./ ec2:/home/ec2-user/BookAI

      - name: 🐳 Rebuild and restart Docker containers on EC2
        run: |
          echo "🚀 Connecting to EC2 and rebuilding containers..."
          ssh ec2 << 'EOF'
            set -e
            cd ~/BookAI

            echo "🧹 Stopping old containers..."
            docker compose down || true

            echo "🧱 Rebuilding Docker images..."
            docker compose build --no-cache

            echo "🚀 Starting containers in detached mode..."
            docker compose up -d

            echo "✅ Deployment successful!"
          EOF
